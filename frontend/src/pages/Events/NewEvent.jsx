// this file is part of the frontend/src/pages/Events directory
// it handles the creation of new events, allowing users to input event details and save them to the database
// it includes a form for entering event title, description, date, and tasks with completion status
import React from 'react'

const NewEvent = () => {
  return (
    <div>
      
    </div>
  )
}

export default NewEvent